global: Welcome to order service prod environment!
message: Welcome to order service prod environment!
server:
  port: 8085
message: Hello from Order Prod!
spring:
  datasource:
    url: jdbc:mysql://localhost:3366/order_prod
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
